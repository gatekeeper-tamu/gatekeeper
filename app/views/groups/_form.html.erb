<%= simple_form_for group do |group_form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
        <% group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= group_form.input :group_name %>

  <%= group_form.association :users, as: :select, collection: User.where.not(id: current_user.id), multiple: true, label_method: :username, input_html: { class: 'chosen-select'} %>
  <%# <fieldset> %>
      <%# <legend>Users:</legend> %>
      <%# <%= group_form.fields_for :members do |users_form| %> 
          <%# <%= render "member_fields", f: users_form %> 
      <%# <% end %> 
      <%# <%= link_to_add_fields "Add Users", group_form, :members %> 
  <%# </fieldset> %>

  <%= group_form.submit "Submit", class: 'btn' %>
  <%= link_to "Cancel", :back, {:class=>"btn btn-default"} %>
<% end %>
